{{- if not .Values.existingSecret }}
{{- fail "existingSecret is required: provision DATABASE_URL, REDIS_URL, JWT_SECRET and ENCRYPTION_KEY via your secret manager" }}
{{- end }}
# Credentials intentionally come from an existing Secret, never generated into Helm history.
